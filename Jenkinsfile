pipeline {
         agent { 
                  Dockerfile true }
            stages {
                 stage('build') {
                    steps {
                        sh 'whoami'
            }
        }
    }
}
