pipeline {
         agent { docker { image 'impavithra/apache:tagname' } }
            stages {
                 stage('build') {
                    steps {
                        sh 'php --version'
            }
        }
    }
}
